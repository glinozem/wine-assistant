# Normalize text files; keep Windows-friendly line endings for PowerShell.
* text=auto

# PowerShell scripts should be CRLF on Windows to avoid editor/tooling churn.
*.ps1 text eol=crlf

# Markdown/docs stay LF for consistency across platforms.
*.md  text eol=lf

# Common config formats
*.yml  text eol=lf
*.yaml text eol=lf
*.json text eol=lf

# Wine Assistant 🍷

[![CI](https://github.com/glinozem/wine-assistant/actions/workflows/ci.yml/badge.svg)](../../actions/workflows/ci.yml)
[![Semgrep](https://github.com/glinozem/wine-assistant/actions/workflows/semgrep.yml/badge.svg)](../../actions/workflows/semgrep.yml)
[![Secrets](https://github.com/glinozem/wine-assistant/actions/workflows/secrets.yml/badge.svg)](../../actions/workflows/secrets.yml)

**Production-ready система управления винным каталогом** с REST API, ETL-пайплайном и расширенными возможностями экспорта данных.

Изначально учебный проект, Wine Assistant вырос в полноценное решение, демонстрирующее best practices современной backend-разработки на Python.

**Текущий статус:** Активная разработка • 165+ тестов • Production-ready архитектура

---

## 🎯 Ключевые возможности

### API & Интеграции
- **REST API** с полной Swagger документацией
- **Защищенные endpoints** с API-key авторизацией
- **Structured JSON logging** с request tracking
- **Health checks** (liveness/readiness) для Kubernetes
- **Rate limiting** для защиты от перегрузки
- **Версионирование API** через URL префиксы

### Управление данными
- **ETL Pipeline** для импорта прайс-листов (Excel/CSV)
- **История цен** с автоматическим версионированием
- **Карантин данных** для невалидных записей
- **Идемпотентность** загрузок через SHA-256 хеши
- **Data Quality Gates** с валидацией перед импортом

### Экспорт и отчеты
- **Множественные форматы:** JSON, Excel (.xlsx), PDF
- **Unicode поддержка** в PDF (кириллица, символ ₽)
- **Фиксированный набор полей** для каждого типа экспорта
- **Эффективный экспорт** больших объемов данных

### Инфраструктура
- **Docker Compose** окружение с PostgreSQL 16
- **Автоматические миграции** БД с версионированием
- **Партиционирование** таблиц для масштабирования
- **CI/CD Pipeline** с GitHub Actions
- **Pre-commit hooks** для проверки кода

---

## 🚀 Быстрый старт

### Вариант 1: Docker Compose (рекомендуется)

```bash
# Клонирование репозитория
git clone https://github.com/glinozem/wine-assistant.git
cd wine-assistant

# Настройка окружения
cp .env.example .env
# Отредактируйте .env при необходимости

# Запуск всего стека
docker compose up -d --build

# Проверка готовности
curl http://localhost:18000/ready
```

**Доступные сервисы:**
- API: http://localhost:18000
- Swagger UI: http://localhost:18000/docs
- Adminer (БД): http://localhost:18080

### Вариант 2: Локальная разработка

```bash
# Создание виртуального окружения
python -m venv .venv

# Активация окружения
# Linux/macOS:
source .venv/bin/activate
# Windows (PowerShell):
.venv\Scripts\Activate
# Windows (cmd):
.venv\Scripts\activate.bat

# Установка зависимостей
pip install -r requirements.txt

# Настройка БД (требуется PostgreSQL 16+)
cp .env.example .env
# Настройте DB_* переменные в .env

# Запуск в режиме разработки
FLASK_ENV=development FLASK_APP=api.wsgi:app flask run
# Альтернативно:
# FLASK_ENV=development FLASK_APP=api.app flask run
```

---

## 📊 Архитектура системы

```
┌──────────────────────────────────────┐
│         Presentation Layer           │
│  • REST API (Flask)                  │
│  • Swagger Documentation             │
│  • Request Middleware & Logging      │
├──────────────────────────────────────┤
│         Business Logic               │
│  • Product Service                   │
│  • Price Management                  │
│  • Export Service                    │
│  • Data Validation (Pydantic)        │
├──────────────────────────────────────┤
│         Data Access Layer            │
│  • PostgreSQL 16 + pgvector          │
│  • Migrations (Alembic-style)        │
│  • Connection Pooling                │
│  • Partitioned Tables                │
└──────────────────────────────────────┘
```

### Основные компоненты

#### 1. API Layer (`/api`)
- `app.py` - Flask приложение с роутингом
- `schemas.py` - Pydantic модели валидации
- `export.py` - Сервис экспорта в различные форматы
- `request_middleware.py` - Request tracking и логирование
- `logging_config.py` - Structured JSON logging
- `validation.py` - Утилиты валидации запросов

#### 2. ETL Layer (`/scripts`)
- `load_csv.py` - Основной ETL pipeline
- `load_utils.py` - Утилиты работы с БД
- `date_extraction.py` - Интеллектуальное извлечение дат
- `idempotency.py` - Проверка дубликатов через хеши

#### 3. Database Layer (`/db`)
- Миграции с версионированием
- Партиционирование по кварталам
- Хранимые процедуры для upsert
- Карантинная таблица для DQ
- Идемпотентные миграции

---

## 🔌 API Endpoints

### Публичные endpoints

```http
GET /live                # Liveness probe
GET /ready                # Readiness probe
GET /api/v1/products      # Список товаров с пагинацией
GET /api/v1/products/search  # Поиск по параметрам
```

### Защищенные endpoints (требуют X-API-Key)

```http
GET /api/v1/sku/{code}                    # Карточка товара
GET /sku/{code}/price-history             # История цен

# Экспорт данных
GET /export/search?format=json|xlsx|pdf                # Экспорт результатов поиска
GET /export/sku/{code}?format=json|pdf                 # Карточка товара
GET /export/price-history/{code}?format=json|xlsx      # История цен по SKU
```

### Примеры использования

```bash
# Получение API ключа из .env
# Простой способ
API_KEY=$(grep API_KEY .env | cut -d '=' -f2)

# Более надежный (убирает пробелы и кавычки)
API_KEY=$(grep API_KEY .env | sed 's/.*=//;s/^[" ]//;s/[" ]$//')

# Поиск товаров (публичный endpoint)
curl "http://localhost:18000/api/v1/products/search?q=Brunello&in_stock=true"

# Карточка товара (требует API key)
curl -H "X-API-Key: $API_KEY" \
  "http://localhost:18000/api/v1/sku/D009704"

# Экспорт в Excel
curl -H "X-API-Key: $API_KEY" \
  "http://localhost:18000/export/search?format=xlsx&limit=100" \
  -o wine_catalog.xlsx

# История цен
curl -H "X-API-Key: $API_KEY" \
  "http://localhost:18000/sku/D009704/price-history"

# Экспорт карточки в PDF
curl -H "X-API-Key: $API_KEY" \
  "http://localhost:18000/export/sku/D009704?format=pdf" \
  -o wine_card_D009704.pdf
```

### 💼 Практический сценарий: менеджер формирует прайс под клиента

**Роль:** менеджер по продажам, который готовит прайс-лист под конкретный запрос клиента.

**Задача:** клиент просит актуальный прайс по винам из Италии в диапазоне 2000–5000 ₽, только то, что есть в наличии.

#### Шаги в веб-интерфейсе

1. **Менеджер открывает веб-интерфейс** поиска каталога.

2. **В форме поиска заполняет фильтры:**
   - Страна: `Италия`
   - Диапазон цен: от `2000` до `5000` ₽
   - Флаг «Только в наличии»: включён (берутся товары с `stock_free > 0`)

3. **Нажимает кнопку «Поиск»** и проверяет, что на экране отображаются подходящие позиции.

4. **Нажимает кнопку «Экспорт»** и выбирает нужный формат:
   - **Excel (XLSX)** — если нужно дальше работать с прайсом, сортировать, фильтровать
   - **PDF** — если прайс нужно сразу отправить клиенту как "красивый" документ

5. **Браузер скачивает файл:**
   - `export_search_*.xlsx` — для Excel
   - `export_search_*.pdf` — для печати или отправки по почте

6. **Менеджер** при необходимости вносит минимальные правки и отправляет файл клиенту.

#### Что происходит под капотом

Фронтенд повторяет те же параметры, которые использовались при поиске:

```http
GET /export/search?format=xlsx&country=Италия&min_price=2000&max_price=5000&in_stock=1
X-API-Key: <секретный ключ>
```

Или для PDF:
```http
GET /export/search?format=pdf&country=Италия&min_price=2000&max_price=5000&in_stock=1
X-API-Key: <секретный ключ>
```

Благодаря этому:
- Данные в файле точно совпадают с тем, что менеджер видит в таблице
- Нет риска "забыть" какой-то фильтр между поиском и экспортом

#### Дополнительный сценарий: карточка товара и история цен

**Задача:** клиент интересуется конкретным SKU и просит подробную информацию и историю цены.

1. Менеджер находит товар по коду или названию (например, `D009704`).

2. На странице карточки товара нажимает:
   - **«Скачать карточку (PDF)»** — фронтенд вызывает `GET /export/sku/D009704?format=pdf` и скачивает `wine_card_D009704.pdf`
   - **«Скачать историю цен (Excel)»** — фронтенд вызывает `GET /export/price-history/D009704?format=xlsx` и скачивает `price_history_D009704.xlsx`

3. Менеджер отправляет клиенту PDF-карточку и Excel с историей цен (например, чтобы показать динамику изменения стоимости).

---

## 📥 Загрузка данных (ETL)

### Импорт прайс-листа из Excel

```bash
# Активация виртуального окружения
source .venv/bin/activate  # Linux/macOS
# или
.venv\Scripts\activate     # Windows

# Настройка подключения к БД
# Linux/macOS:
export DB_HOST=localhost
export DB_PORT=15432
export DB_USER=postgres
export DB_PASSWORD=postgres
export DB_NAME=wine_db

# Windows (PowerShell):
$env:DB_HOST="localhost"
$env:DB_PORT="15432"
$env:DB_USER="postgres"
$env:DB_PASSWORD="postgres"
$env:DB_NAME="wine_db"

# Windows (cmd):
set DB_HOST=localhost
set DB_PORT=15432
set DB_USER=postgres
set DB_PASSWORD=postgres
set DB_NAME=wine_db

# Загрузка прайс-листа
python -m scripts.load_csv --excel "data/price_list.xlsx"

# Или через Makefile
make load-price EXCEL_PATH="./data/price_list.xlsx"
```

### Поддерживаемые форматы
- Excel (.xlsx, .xls)
- CSV с разделителями (`,`, `;`, `\t`)
- Автоопределение колонок и форматов
- Извлечение даты из имени файла

### Data Quality проверки
- Валидация обязательных полей
- Проверка диапазонов цен и скидок
- Контроль дубликатов через SHA-256
- Карантин для невалидных записей

---

## 🧪 Тестирование

### Запуск всех тестов

```bash
# Полный прогон (unit + integration)
pytest -q -rs

# Только unit-тесты
pytest tests/unit -q

# Прогон с БД-зависимыми тестами
# Linux/macOS:
export RUN_DB_TESTS=1
pytest -q -rs

# Windows (PowerShell):
$env:RUN_DB_TESTS="1"
pytest -q -rs

# Windows (cmd):
set RUN_DB_TESTS=1
pytest -q -rs

# Интеграционные тесты (альтернативный способ, Linux/macOS)
export RUN_DB_TESTS=1
pytest -m integration -vv

# С покрытием
pytest --cov=api --cov=scripts --cov-report=html
```

### Метрики тестирования
- **165+ тестов** всех уровней
- **Unit тесты:** валидация, схемы, утилиты
- **Integration тесты:** API + БД взаимодействие
- **E2E тесты:** полный цикл загрузки и экспорта
- **Coverage:** >80% для критического кода

---

## 🏗️ Development Setup

### Использование Makefile

```bash
# Основные команды
make dev-up          # Поднять окружение
make dev-down        # Остановить контейнеры
make db-reset        # Пересоздать БД с нуля
make test-unit       # Unit-тесты
make test-int        # Интеграционные тесты
make check           # Линтер + все тесты

# Работа с данными
make load-price EXCEL_PATH="./data/price.xlsx"  # Загрузить прайс
make show-quarantine  # Просмотр карантина

# Дополнительные команды
make lint            # Запуск линтера (ruff)
make format          # Форматирование кода
make clean           # Очистка временных файлов
```

### Pre-commit hooks

```bash
# Установка хуков
pre-commit install

# Ручной запуск
pre-commit run --all-files

# Обновление хуков
pre-commit autoupdate
```

---

## 📈 Мониторинг и метрики

### Health Checks

```bash
# Liveness - приложение живо
curl http://localhost:18000/live

# Readiness - готово принимать запросы
curl http://localhost:18000/ready
```

### Structured Logging

Все логи в JSON формате с контекстом:

```json
{
  "timestamp": "2025-11-24T10:30:45.123Z",
  "timestamp_unix": 1732445445.123,
  "level": "INFO",
  "request_id": "req_20251124_103045_a1b2c3d4",
  "method": "GET",
  "path": "/api/v1/products/search",
  "duration_ms": 45.3,
  "status_code": 200,
  "client_ip": "127.0.0.1",
  "user_agent": "curl/7.68.0"
}
```

### Метрики производительности

- Время ответа API: p95 < 100ms
- Пропускная способность: 1000+ RPS
- Размер БД: партиционирование по кварталам
- Кеш hit rate: планируется Redis

---

## 🗺️ Roadmap

### ✅ Реализовано (Sprint 1-7)
- [x] REST API с Swagger документацией
- [x] ETL pipeline для прайс-листов
- [x] Система версионирования цен
- [x] Карантин для невалидных данных (Issue #84)
- [x] Data Quality Gates (Issue #83)
- [x] Экспорт в Excel/PDF/JSON (Issue #69)
- [x] Structured JSON logging
- [x] Request tracking с уникальными ID
- [x] Партиционирование таблиц (Issue #85)
- [x] Rate limiting (Flask-Limiter) для ключевых эндпоинтов
- [x] Идемпотентные миграции БД
- [x] Unit тесты для ETL (Issue #91)

### 🚧 В разработке (Sprint 8)
- [ ] Векторный поиск с эмбеддингами (Issue #68)
- [ ] Telegram-бот для поиска вин (Issue #67)
- [ ] Примеры клиентов (Python, JavaScript) (Issue #66)

### 📋 Планируется (Sprint 9+)
- [ ] Prometheus метрики (Issue #63)
- [ ] Redis кеширование
- [ ] Performance тесты (Load Testing) (Issue #61)
- [ ] E2E тесты критических сценариев (Issue #60)
- [ ] Integration тесты API endpoints (Issue #59)
- [ ] Sentry для Error Tracking (Issue #64)
- [ ] WebSocket для real-time обновлений
- [ ] GraphQL API
- [ ] ML-based рекомендации
- [ ] Async задачи через Celery
- [ ] Kubernetes манифесты
- [ ] Multi-tenancy поддержка

Подробный roadmap: [`docs/ROADMAP_v3_RU.md`](docs/ROADMAP_v3_RU.md)

---

## 🤝 Contribution

### Процесс разработки

1. Создайте feature branch от `master`
   ```bash
   git checkout -b feature/your-feature
   ```
2. Напишите код с тестами
3. Убедитесь в прохождении проверок:
   ```bash
   make check  # линтер + тесты
   ```
4. Создайте Pull Request с описанием изменений

### Стандарты кода
- Python 3.11+
- Type hints обязательны
- Docstrings для публичных функций
- Покрытие тестами >80%
- Ruff для линтинга
- Black для форматирования

### Коммиты
- Используйте conventional commits
- Примеры: `feat:`, `fix:`, `docs:`, `test:`, `refactor:`
- Ссылайтесь на Issues: `(Refs #123)`

---

## 📚 Документация

- [Архитектура проекта](docs/architecture.md)
- [API спецификация](http://localhost:18000/docs)
- [Руководство по ETL](docs/etl-guide.md)
- [Экспорт каталога через веб-интерфейс](docs/export-web-ui.md)
- [Windows Setup](docs/dev/dev-setup-windows.md)
- [Шпаргалка: от чистой среды до зелёных тестов](docs/dev/cheatsheet-from-clean-clone-to-green-tests.md)
- [Issue #83-84: Data Quality Gates](docs/dev/issue-83-84-notes.md)
- [Issue #85: Партиционирование](docs/dev/issue-85-partitioning-notes.md)
- [Troubleshooting](docs/troubleshooting.md)

---

## 🔒 Безопасность

- **API Key авторизация** для защищенных endpoints
- **Валидация входных данных** через Pydantic
- **SQL injection защита** (параметризованные запросы)
- **Secrets scanning** в CI/CD (Gitleaks)
- **Security headers** в production
- **Rate limiting** для защиты от перегрузки
- **Dependency scanning** через pip-audit
- **SAST** через Semgrep

---

## 📊 Производительность

- **Connection pooling** для БД
- **Партиционирование** таблиц по кварталам
- **Индексы** для поиска (GIN для полнотекстового)
- **Эффективный экспорт** больших объемов данных
- **Pagination** по умолчанию (limit/offset)
- **Bulk operations** для массовых загрузок
- **Prepared statements** для частых запросов

---

## 📦 Технологический стек

| Категория | Технологии |
|-----------|------------|
| **Backend** | Python 3.11+, Flask, Pydantic |
| **Database** | PostgreSQL 16, pgvector, pg_trgm |
| **Infrastructure** | Docker, Docker Compose, Gunicorn |
| **Testing** | pytest, coverage, integration tests |
| **CI/CD** | GitHub Actions, Semgrep, pip-audit |
| **Monitoring** | Structured logging, Health checks |
| **Documentation** | Swagger/OpenAPI, Markdown |
| **Code Quality** | Ruff, Black, Pre-commit hooks |
| **Export** | openpyxl, ReportLab (PDF), JSON |

---

## 📄 Лицензия

MIT License - см. файл [LICENSE](LICENSE)

Проект можно свободно использовать как основу для собственных решений и обучения.

---

## 👥 Команда и контакты

**Maintainer:** [@glinozem](https://github.com/glinozem)

**Статус проекта:** Активная разработка

**Последнее обновление:** Ноябрь 2025

---

<p align="center">
  <i>Wine Assistant - от учебного проекта к production-ready системе</i>
</p>
